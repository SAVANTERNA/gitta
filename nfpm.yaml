name: gitta
arch: amd64
platform: linux
version: 0.1.0
section: utils
priority: optional
maintainer: "gg <apt@njb.gg>"
description: |
  gitta — placeholder Python CLI. Replace this with a real description.
homepage: https://gegge01.github.io/gitta
license: GPL-3.0
depends:
  - python3
  - bash
vendor: gitta
overrides:
  deb:
    fields:
      Homepage: https://gegge01.github.io/gitta
      Vcs-Browser: https://github.com/gegge01/gitta
      Vcs-Git: https://github.com/gegge01/gitta.git
contents:
  # Install Python module into Debian's dist-packages so `python3 -m gitta` works
  - src: ./src/gitta
    dst: /usr/lib/python3/dist-packages/gitta
  # CLI launcher script into PATH
  - src: ./assets/gitta
    dst: /usr/bin/gitta
    file_info:
      mode: 0755
  # Manpage (optional for now)
  - src: ./assets/gitta.1
    dst: /usr/share/man/man1/gitta.1
